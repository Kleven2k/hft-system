System Status
Built & hw-validated: market data pipeline, order book (4 slots), strategy (OMS/risk/skew/EMA/P&L), telemetry dashboard, UART config

Gaps found in scan:

Timing: WNS = -1.928 ns (pre-existing, hw works but technically over-budget)
best_bid_qty/best_ask_qty come out of the order book but are never wired to 
strategy — the alpha signal building blocks are sitting there unused
UART TX = echo loopback only, no readback path
ARP uses broadcast MAC (works but fragile)
Symbol routing is hardcoded identity (symbol_id must equal slot index)
tb/order_engine/ and tb/market_data/ are empty — no end-to-end simulation
Rust backtester in research-tools/ produces optimal params but nothing connects it to the FPGA

New Roadmap
Phase	Description	What to build

23	Timing closure	Register skewed_bid[i]/skewed_ask[i] per-book (same pattern as fat_finger_r). 
Breaks the rr_idx → skewed_bid arithmetic → order_out.price path (current worst offender at -1.928 ns). 
Clean bitfile, no functional change.

24	Book depth alpha signal	Wire best_bid_qty[N_BOOKS]/best_ask_qty[N_BOOKS] from order_book to strategy. 
Add per-book imbalance flags: if bid_qty > IMBAL_SHIFT× ask_qty → suppress BUY; reverse for SELL. 
Uses shift comparison (no division). Mirrors the top_of_book_imbalance already in orderbook.py.

25	Latency measurement	Add a free-running 64-bit counter in clk domain. 
Stamp at 4 pipeline stages: (1) quote_valid out of itch_msg_bridge, (2) bid_valid first asserted by order_book, 
(3) order_valid out of strategy, (4) ack_valid back into strategy. 
Report min/avg/max deltas in the telemetry UDP packet (expand from 64 → 96 bytes). 
Python monitor.py prints latency breakdown. Target: market data → order out < 1 µs.

26	System testbench	Fill tb/order_engine/ and tb/market_data/. 
A single cocotb test that drives raw UDP bytes in on the ITCH port and asserts the correct OUCH bytes come out, 
exercising the full CDC chain. This is the safety net needed before the session layer rewrite.

27	OUCH 4.2 session layer	Replace the current bare-frame ouch_encoder with a proper 
NASDAQ OUCH 4.2 session: Login/Logout messages, sequence numbers on every outbound message, Heartbeat every 1s when idle, 
server-heartbeat timeout watchdog. Requires a session FSM (LOGGED_OUT → LOGGING_IN → ACTIVE → LOGGING_OUT) and 
a sequence counter in the encoder. Enables connecting to a real exchange simulator (e.g., NASDAQ OUCH test server). 
This is the production gate.

28	Configurable symbol mapping	Replace symbol_router's identity cast with a UART-programmable 4-entry lookup table: 
symbol_map[slot] = symbol_id. A new UART frame type (e.g., `0xC0

29	Research → hardware parameter loop	The Rust backtester in research-tools/market_engine/ already outputs 
fills.csv/market.csv. Write a Python script that reads backtester output and emits the optimal SPREAD_MAX, SKEW_SHIFT, 
FAT_FINGER_BPS, SPREAD_EMA_SHIFT as UART config commands to the FPGA. Closes the research-to-production loop.